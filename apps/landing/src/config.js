// URL de la aplicación principal (donde está el login)
// En desarrollo: localhost:3000
// En producción: app.tudominio.com
const APP_URL = process.env.REACT_APP_URL || 'http://localhost:3000';

export default APP_URL;
